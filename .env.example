# Supabase (anon key is safe for the frontend when RLS is enabled).
# NEVER put the SERVICE ROLE key in the frontend or in any VITE_ variable.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_EMAIL=suporte@gasfinder.com
VITE_VAPID_PUBLIC_KEY=
